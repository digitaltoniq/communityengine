%dl.vcard
  %dt.photo
    = link_to image_tag(user.avatar_photo_url(:small), :alt => "#{user}"), user_path(user)
  %dd.fn
    =link_to user, user_path(user), :class => 'url'
  %dd
    - if user.description
      %p= truncate_words( user.description, 9, '...')
