-# DJS TODO .yui-b= render :partial => 'users/profile_user_info_sidebar'

#yui-main
  .yui-b
    -box :id => "friends" do
      %h3
        = @following_count
        =:accepted_friendships.l
        -# DJS TODO = link_to "(#{@pending_friendships_count} "+:pending.l+")", pending_user_friendships_path(@user) if  (@pending_friendships_count > 0)
      .pagination=paginating_links @followings if @followings.page_count > 1

      .clear
      .user_list= render :partial => "followers/follower", :collection => followers

