%dl.vcard
  %dt.photo
    = link_to image_tag((post.first_image_in_body || post.user.avatar_photo_url(:thumb)), :alt=>"", :width=>"50", :height=>"50"), post_path(post)
  %dd.fn
    = link_to post.title, post_path(post), :rel => 'bookmark'
  %dd= truncate_words(post.post, 9, '...' )
