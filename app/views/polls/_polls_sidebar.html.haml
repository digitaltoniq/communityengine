.clear
%br
.padded
  %h2= :popular_polls.l
  %ul
    -polls.each do |poll|
      %li
        =link_to "#{truncate(poll.question, :length => 75)} <small class='quiet'>(#{pluralize poll.votes.count, :votes.l})</small>", post_path(poll.post)
